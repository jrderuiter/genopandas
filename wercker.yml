box:
  id: continuumio/miniconda3
  tag: latest

test:
  steps:
    - install-packages:
      packages: make
    - script:
      name: setup env
      code: |
        make env
        source activate genopandas-dev
        pip install .
    - script:
      name: pytest
      code: make test
